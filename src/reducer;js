export const initialState = {
    basket: [],
    user: null
};


//try making new file and taking export default 

function reducer(state, action) {
    switch(action.type) {
        case 'ADD_TO_BASKET':
            //adding item to basket
            break;
        case 'REMOVE_FROM_BASKET':
            //removing from basket 
            break;
        default:
            return state;
    }
}

export default reducer;